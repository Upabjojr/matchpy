# -*- coding: utf-8 -*-
# =============================================================================
# !! AUTO-GENERATED FILE -- DO NOT EDIT !!
#
# Generated by: rubi_rules/codegen/generate.py
# Source: 1.1.2.1 (a+b x^2)^p.m
# Module: 1.1.2.1 (a+b x^2)^p
#
# This file contains Rubi integration rules as RubiRulePattern objects.
# Re-run the generator to update.
# =============================================================================
import sympy
from sympy import Integer, Integral, Lambda, Rational, Symbol, Tuple as SympyTuple, Eq, Ne, Lt, Gt, Le, Ge, log, sqrt, pi, I, oo
from rubi_rules.utils.rubi_utils import *  # bare-name access; sympy imports below override any conflicts (e.g. Not)
from sympy.logic.boolalg import Or, Not, And
from sympy import (sin, cos, tan, sec, csc, cot, asin, acos, atan, atan2, asec, acsc, acot,
                   sinh, cosh, tanh, sech, csch, coth, asinh, acosh, atanh, asech, acsch, acoth,
                   exp, Abs, diff, denom, frac, floor, root, simplify,
                   elliptic_e, elliptic_f, hyper, appellf1)

from sympy_objects.wild import WildSymbol, IDENTITY_ELEMENT
from rubi_rules.base_objects import Int, RubiRulePattern
from rubi_rules.utils import (
    # Wolfram standard constraints
    FreeQ, IntegerQ, OddQ, EvenQ, NumberQ, NumericQ, AtomQ, MemberQ,
    PositiveQ, NegativeQ, PolynomialQ, TrueQ, FalseQ, MatchQ, PrimeQ, UnsameQ,
    # RUBI-specific constraints
    EqQ, NeQ, IGtQ, ILtQ, IGeQ, ILeQ, GtQ, LtQ, GeQ, LeQ, PosQ, NegQ,
    IntegersQ, HalfIntegerQ, FractionQ, RationalQ, ComplexNumberQ, RealNumberQ,
    FractionOrNegativeQ, SqrtNumberQ, PowerQ, ProductQ, SumQ, NonsumQ,
    IntegerPowerQ, FractionalPowerQ, PolyQ, LinearQ, QuadraticQ, BinomialQ,
    TrinomialQ, LinearMatchQ, QuadraticMatchQ, BinomialMatchQ, TrinomialMatchQ,
    TrigQ, HyperbolicQ, InverseTrigQ, InverseHyperbolicQ, LogQ, ComplexFreeQ,
    InverseFunctionFreeQ, FractionalPowerFreeQ, TrigHyperbolicFreeQ, IntegralFreeQ,
    RationalFunctionQ, AlgebraicFunctionQ, IndependentQ, SimplerQ, SumSimplerQ,
    FunctionOfQ, PiecewiseLinearQ, ExpressionEqQ,
    IntLinearQ, IntBinomialQ, IntQuadraticQ,
    MonomialQ, LinearPairQ,
    GeneralizedBinomialQ, GeneralizedBinomialMatchQ,
    GeneralizedTrinomialQ, GeneralizedTrinomialMatchQ,
    NiceSqrtQ, SimplerSqrtQ, FractionalPowerFactorQ,
    SumBaseQ, InverseFunctionQ, InertTrigQ, InertTrigFreeQ,
    CalculusFreeQ, QuotientOfLinearsQ,
    PowerOfLinearQ, PowerOfLinearMatchQ,
    FunctionOfExponentialQ,
    KnownSineIntegrandQ, KnownSecantIntegrandQ,
    KnownTangentIntegrandQ, KnownCotangentIntegrandQ,
    EulerIntegrandQ, SubstForFractionalPowerQ,
    PerfectSquareQ, PolynomialInQ, FunctionOfTrigOfLinearQ,
    SimplerIntegrandQ, PseudoBinomialPairQ, QuadraticProductQ,
    EveryQ, TrigSimplifyQ, EqM, TryPureTanSubst,
)

# --- Integration variable ---
x = Symbol('x')

# --- Rubi global option stubs (default False/placeholder) ---
UseGamma = sympy.Symbol('UseGamma')  # Rubi global option; treated as False in Python
u = Symbol('u')  # Generic integrand placeholder used in some Rubi constraint calls

# --- Wildcard symbols ---
# dot wildcards (must match exactly one expression)
# optional wildcards (can match identity element if absent in commutative ops)

_a_ = WildSymbol('a', optional_value=IDENTITY_ELEMENT)
a_ = WildSymbol('a')
_b_ = WildSymbol('b', optional_value=IDENTITY_ELEMENT)
b_ = WildSymbol('b')
n_ = WildSymbol('n')
p_ = WildSymbol('p')
v_ = WildSymbol('v')

RULES = [
    # Rule 1: SKIPPED - ValueError: Non-string function head ['Times', '2', ['FracPart', 'p']] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 2
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**(sympy.S(-3)/2), x),
        constraints=(FreeQ([a_, _b_], x),),
        replacement=x/(a_*sqrt(x**2*_b_ + a_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=2,
    ),
    # Rule 3: SKIPPED - ValueError: Non-string function head ['Times', '2', 'a', ['Plus', 'p', '1']] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 4
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**p_, x),
        constraints=(FreeQ([a_, _b_], x), IGtQ(p_, 0),),
        replacement=Int(ExpandIntegrand((x**2*_b_ + a_)**p_, x), x),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=4,
    ),
    # Rule 5: SKIPPED - ValueError: Non-string function head ['Plus', ['Times', '2', 'p'], '1'] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 6
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**(sympy.S(-5)/4), x),
        constraints=(FreeQ([a_, _b_], x), GtQ(a_, 0), PosQ(_b_/a_),),
        replacement=2*elliptic_e(atan(x*sqrt(_b_/a_))/2, 2)/(a_**(sympy.S(5)/4)*sqrt(_b_/a_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=6,
    ),
    # Rule 7: SKIPPED - ValueError: Non-string function head ['Times', 'a', ['Power', ['Plus', 'a', ['Times', 'b', ['Power', 'x', '2']]], ['Times', '1', ['Power', '4', '-1']]]] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 8: SKIPPED - ValueError: Non-string function head ['Times', ['Power', ['Plus', 'a', ['Times', 'b', ['Power', 'x', '2']]], ['Times', '2', ['Power', '3', '-1']]], ['Power', ['Times', 'a', ['Power', ['Plus', 'a', ['Times', 'b', ['Power', 'x', '2']]], '-1']], ['Times', '2', ['Power', '3', '-1']]]] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 9: SKIPPED - ValueError: Non-string function head ['Times', '2', 'a', ['Plus', 'p', '1']] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 10
    RubiRulePattern(
        pattern=Int(1/(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), PosQ(a_/_b_), Or(GtQ(a_, 0), GtQ(_b_, 0)),),
        replacement=atan(x*sqrt(_b_)/sqrt(a_))/(sqrt(a_)*sqrt(_b_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=10,
    ),
    # Rule 11
    RubiRulePattern(
        pattern=Int(1/(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), PosQ(a_/_b_), Or(LtQ(a_, 0), LtQ(_b_, 0)),),
        replacement=-atan(x*sqrt(-_b_)/sqrt(-a_))/(sqrt(-a_)*sqrt(-_b_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=11,
    ),
    # Rule 12
    RubiRulePattern(
        pattern=Int(1/(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), PosQ(a_/_b_),),
        replacement=sqrt(a_/_b_)*atan(x/sqrt(a_/_b_))/a_,
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=12,
    ),
    # Rule 13
    RubiRulePattern(
        pattern=Int(1/(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), NegQ(a_/_b_), Or(GtQ(a_, 0), LtQ(_b_, 0)),),
        replacement=atanh(x*sqrt(-_b_)/sqrt(a_))/(sqrt(a_)*sqrt(-_b_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=13,
    ),
    # Rule 14
    RubiRulePattern(
        pattern=Int(1/(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), NegQ(a_/_b_), Or(LtQ(a_, 0), GtQ(_b_, 0)),),
        replacement=-atanh(x*sqrt(_b_)/sqrt(-a_))/(sqrt(_b_)*sqrt(-a_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=14,
    ),
    # Rule 15
    RubiRulePattern(
        pattern=Int(1/(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), NegQ(a_/_b_),),
        replacement=sqrt(-a_/_b_)*atanh(x/sqrt(-a_/_b_))/a_,
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=15,
    ),
    # Rule 16
    RubiRulePattern(
        pattern=Int(1/sqrt(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), GtQ(a_, 0), PosQ(_b_),),
        replacement=asinh(x*sqrt(_b_)/sqrt(a_))/sqrt(_b_),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=16,
    ),
    # Rule 17
    RubiRulePattern(
        pattern=Int(1/sqrt(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), GtQ(a_, 0), NegQ(_b_),),
        replacement=asin(x*sqrt(-_b_)/sqrt(a_))/sqrt(-_b_),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=17,
    ),
    # Rule 18
    RubiRulePattern(
        pattern=Int(1/sqrt(x**2*_b_ + a_), x),
        constraints=(FreeQ([a_, _b_], x), Not(GtQ(a_, 0)),),
        replacement=Subst(Int(1/(-x**2*_b_ + 1), x), x, x/sqrt(x**2*_b_ + a_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=18,
    ),
    # Rule 19
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**(sympy.S(-1)/4), x),
        constraints=(FreeQ([a_, _b_], x), GtQ(a_, 0), PosQ(_b_/a_),),
        replacement=((Integer(2) * x * (((a_ + (_b_ * (x)**(Integer(2)))))**((Integer(4))**(Integer(-1))))**(Integer(-1))) + (Integer(-1) * (sympy.Function('a')(Symbol('Star')) * Int((((a_ + (_b_ * (x)**(Integer(2)))))**((Integer(5) * (Integer(4))**(Integer(-1)))))**(Integer(-1)), x)))),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=19,
    ),
    # Rule 20
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**(sympy.S(-1)/4), x),
        constraints=(FreeQ([a_, _b_], x), GtQ(a_, 0), NegQ(_b_/a_),),
        replacement=2*elliptic_e(asin(x*sqrt(-_b_/a_))/2, 2)/(a_**(sympy.S(1)/4)*sqrt(-_b_/a_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=20,
    ),
    # Rule 21: SKIPPED - ValueError: Non-string function head ['Times', '1', ['Power', '4', '-1']] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 22: SKIPPED - ValueError: Non-string function head ['Times', 'b', 'x'] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 23
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**(sympy.S(-3)/4), x),
        constraints=(FreeQ([a_, _b_], x), GtQ(a_, 0), PosQ(_b_/a_),),
        replacement=2*elliptic_f(atan(x*sqrt(_b_/a_))/2, 2)/(a_**(sympy.S(3)/4)*sqrt(_b_/a_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=23,
    ),
    # Rule 24
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**(sympy.S(-3)/4), x),
        constraints=(FreeQ([a_, _b_], x), GtQ(a_, 0), NegQ(_b_/a_),),
        replacement=2*elliptic_f(asin(x*sqrt(-_b_/a_))/2, 2)/(a_**(sympy.S(3)/4)*sqrt(-_b_/a_)),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=24,
    ),
    # Rule 25: SKIPPED - ValueError: Non-string function head ['Times', '3', ['Power', '4', '-1']] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 26: SKIPPED - ValueError: Non-string function head ['Times', 'b', 'x'] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 27: SKIPPED - ValueError: Non-string function head ['Times', '2', 'b', 'x'] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 28: SKIPPED - ValueError: Non-string function head ['Times', '2', 'b', 'x'] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 29
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**(sympy.S(-1)/6), x),
        constraints=(FreeQ([a_, _b_], x),),
        replacement=((Integer(3) * x * ((Integer(2) * ((a_ + (_b_ * (x)**(Integer(2)))))**((Integer(6))**(Integer(-1)))))**(Integer(-1))) + (Integer(-1) * (a_ * (sympy.Function('2')(Symbol('Star')))**(Integer(-1)) * Int((((a_ + (_b_ * (x)**(Integer(2)))))**((Integer(7) * (Integer(6))**(Integer(-1)))))**(Integer(-1)), x)))),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=29,
    ),
    # Rule 30: SKIPPED - ValueError: Non-string function head ['Times', ['Power', ['Times', 'a', ['Power', ['Plus', 'a', ['Times', 'b', ['Power', 'x', '2']]], '-1']], ['Times', '1', ['Power', '3', '-1']]], ['Power', ['Plus', 'a', ['Times', 'b', ['Power', 'x', '2']]], ['Times', '1', ['Power', '3', '-1']]]] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 31
    RubiRulePattern(
        pattern=Int((x**2*_b_ + a_)**p_, x),
        constraints=(FreeQ([a_, _b_, p_], x), Not(IntegerQ(2*p_)), GtQ(a_, 0),),
        replacement=x*a_**p_*hyper((sympy.S.Half, -p_), (sympy.S(3)/2,), -x**2*_b_/a_),
        module_name='1.1.2.1 (a+b x^2)^p',
        rule_number=31,
    ),
    # Rule 32: SKIPPED - ValueError: Non-string function head ['FracPart', 'p'] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.
    # Rule 33: SKIPPED - ValueError: Non-string function head ['Coefficient', 'v', 'x', '1'] -- function-head wildcard patterns (e.g., F_[x_]) are not yet supported.

]

# Summary: 18 rules translated, 15 skipped
